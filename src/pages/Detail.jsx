import React from "react";
import { WithRouter } from "../utils/Navigation";

const Detail = () => {
  return (
    <div>
      <header>Ini Detail</header>
    </div>
  );
};

export default WithRouter(Detail);
